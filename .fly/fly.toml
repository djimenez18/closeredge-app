# .fly/fly.toml — closeredge-core on Fly.io
#
# See gitbooks/features/cloud-deploy.md for the full deployment guide.
# See https://fly.io/docs/reference/configuration/ for fly.toml reference.
#
# Quickstart:
#   fly launch --no-deploy --config .fly/fly.toml
#   fly volumes create closeredge_workspace --size 5 --region <region> --config .fly/fly.toml
#   fly secrets set CLOSEREDGE_CORE_TOKEN="$(openssl rand -hex 32)"
#   fly secrets set BACKEND_URL="https://api.closeredge.ai"
#   fly deploy --config .fly/fly.toml

app = '<your-app-name>'
primary_region = '<your-region>'

[build]
  dockerfile = "Dockerfile"

[env]
  CLOSEREDGE_CORE_HOST = "0.0.0.0"
  CLOSEREDGE_CORE_PORT = "7788"
  CLOSEREDGE_WORKSPACE = "/home/closeredge/.closeredge"
  RUST_LOG = "info"

[[mounts]]
  source = "closeredge_workspace"
  destination = "/home/closeredge/.closeredge"

[http_service]
  internal_port = 7788
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  # min_machines_running = 0 fully stops the machine when idle (cheapest), but
  # the first request after idle pays a cold-start penalty (container boot +
  # Rust binary init — several seconds). Set to 1 to keep one machine warm.
  min_machines_running = 0
  processes = ['app']

  [[http_service.checks]]
    interval = "30s"
    timeout = "5s"
    grace_period = "10s"
    method = "GET"
    path = "/health"

[[vm]]
  memory = '1gb'
  cpus = 1
